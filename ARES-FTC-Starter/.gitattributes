# The ARES generator compares checked-in mechanical Kotlin to deterministic LF output.
# Keep this file byte-stable on Windows so a fresh checkout passes verifyAresProject.
/TeamCode/src/main/java/org/firstinspires/ftc/teamcode/generated/GeneratedAresProject.kt text eol=lf
gradlew text eol=lf
gradlew.bat text eol=crlf
